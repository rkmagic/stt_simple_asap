# Reproducible installs (use npm ci in CI/Docker)
package-lock=true

# Prefer exact versions in package.json when adding deps
save-exact=true

# Fail install if Node engine in package.json does not match
engine-strict=true

# Audit policy (npm audit in verify script / Docker build)
audit=true
audit-level=moderate

fund=false
